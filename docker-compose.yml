version: '3.8'

services:
  flycast-webrcade:
    build: .
    container_name: flycast-webrcade
    ports:
      - "3333:3000"
    environment:
      # Use the provided Archive.org collection for ROMs
      - ARCHIVE_ITEM_ID=sega-dreamcast-redump-collection
    restart: unless-stopped
